# FE-879 작업 브리프

> 현재 브랜치 `develop`에는 티켓 번호가 없어 `FE-879`를 임시 작업번호로 사용한다.
> 실제 이슈 트래커 번호 확인이 필요하다.

## 1. 프로젝트 정보

- 주 저장소: `/Users/jaden/workspace/naver-connect-plugin-test` (Next.js 16)
- 연동 저장소: `/Users/jaden/workspace/payssam-front-manager` (Vue 2)
- 대상: Vue2 사업자 화면에서 발생한 이벤트를 동일 사업자의 여러 Next.js 단말 화면에 전달하는 SSE PoC
- 작업 브랜치: `develop` (Next.js), `feature/naver-connect-test` (Vue2)
- 현재 기준 commit: Next.js `78950a4` (다중 브라우저 테스트 작업 시작 전)
- 시작 전 범위 밖 변경: Next.js 저장소의 `AGENTS.md` 수정은 사용자 기존 변경으로 보존
- local commit / push: Next.js `78950a4` / 미수행

## 2. 목표와 배경

### 목표

1. 단말기 화면 on/off 또는 WebView 백그라운드 전환으로 SSE가 조용히 끊겨도 자동 복구한다.
2. 연결이 끊긴 동안 발생한 마지막 이벤트를 재연결 시 따라잡는다.
3. 한 사업자(Vue2)와 여러 단말기(Next.js)를 `merchantCode` 기준 1:N 관계로 묶는다.
4. 이벤트, revision, lastEvent가 다른 사업자와 섞이지 않도록 격리한다.
5. 실제 어드민·세션 API가 없는 PoC에서는 비동기 mock API로 검증 흐름을 표현한다.

### 기존 문제

- heartbeat와 활동 시각 추적이 없어 죽은 SSE 연결을 감지하지 못할 수 있었다.
- 모든 SSE 클라이언트와 최신 상태가 전역 단일 그룹이어서 사업자별 격리가 없었다.
- 단말 소속과 Vue2 로그인 사업자를 확인할 API 계약이 없었다.
- 객체 Map을 API 대체물로 사용하면 실제 비동기 호출 경계가 불명확했다.

## 3. 최종 범위

### 포함

- SSE heartbeat(15초), 클라이언트 watchdog(10초 검사, 45초 무응답 판정)
- 연결 상태 `identifying / connecting / connected / error / device-error` 구분
- 재연결 전 기존 EventSource 정리 및 2초 후 재시도
- 최초 연결 baseline 동기화와 재연결/화면 복귀 catch-up
- `visibilitychange` 포그라운드 복귀 처리
- 단말 `deviceSerialNo`를 이용한 소유 `merchantCode` 조회
- Vue2 Bearer token을 이용한 로그인 `merchantCode` 조회
- `Map<merchantCode, MerchantCodeChannel>` 기반 1:N 단말 그룹
- merchant별 clients, revision, lastEvent 분리
- 비동기 mock admin/session API
- Vue2 테스트 요청에 PoC Authorization 헤더 추가
- 기존 전역 N:N 방식과 변경된 사업자별 1:N 방식의 통신 흐름 SVG, 코드 줄별 실행 의미·상태·Ref·실행 순서·점검 사항을 설명하는 HTML 문서와 `/docs` 목록 연결
- 구현 이유 중심 한국어 주석
- 같은 사업자 단말 2대와 다른 사업자 단말 1대를 브라우저별 쿼리로 선택하는 PoC 테스트 값
- Vue2 테스트 화면에서 `Authorization` 쿼리로 mock 발신 토큰 A/B 선택

### 제외

- 실제 어드민 API 및 Payssam 세션/JWT 검증 연동
- Redis/PubSub 등 멀티 인스턴스 공용 상태 저장소
- 강한 단말 인증(서명, 인증서, 단말 전용 토큰)
- 단말 수신 완료 ACK
- 운영 CORS/CSRF 정책
- 실제 결제 도메인 로직

## 4. 최종 설계

### 테스트 값

| 구분          | 값                        |
| ------------- | ------------------------- |
| 단말 시리얼   | `deviceSerialNoMockData`  |
| 사업자 코드   | `merchantCodeMockData`    |
| Vue2 PoC 토큰 | `test-merchant-token-001` |

현재 `app/page.tsx`는 로컬 테스트를 위해 `deviceSerialNoMockData`를 고정 사용한다. 실제 WebView 적용 시
주석 처리된 `window.npayContext?.deviceSerialNo?.trim()`으로 교체한다.

다중 브라우저 PoC에서는 단말 URL의 기존 `deviceSerialNo` 키가 있으면 고정값 대신 사용한다.
`A-device-1`, `A-device-2`는 `merchantCodeMockData`, `B-device-1`은 `merchantCodeMockDataB`로
mock admin API에서 매핑한다. 쿼리가 없으면 기존 고정값을 유지한다.

### 단말기 환경(Next.js WebView)

1. 페이지 로드 시 `deviceSerialNo`를 확보한다.
2. `GET /api/connect/state?deviceSerialNo=...`로 등록 여부와 현재 상태를 확인한다.
3. 서버는 mock admin API를 비동기 fetch하여 `merchantCode`를 얻는다.
4. 등록 단말이면 `GET /api/connect/events?deviceSerialNo=...`에 EventSource로 연결한다.
5. `/events`도 같은 소속 검증 후 해당 merchant 채널에 client를 등록한다.
6. `display.changed` 수신 시 더 높은 revision만 화면에 반영한다.
7. 재연결 또는 화면 복귀 시 `/state`에서 같은 merchant의 lastEvent를 조회한다.

단말기는 사업자 로그인 토큰이나 merchantCode를 전달하지 않는다.

### 사업자 환경(Vue2 manager)

1. 사용자가 테스트 버튼을 누른다.
2. `POST /api/connect/notify`에 message와 `Authorization: Bearer test-merchant-token-001`을 보낸다.
3. `/notify`는 mock session API를 비동기 fetch하여 검증된 `merchantCode`를 얻는다.
4. 요청 body의 merchantCode는 사용하지 않는다.
5. 검증된 merchant 채널에만 이벤트를 브로드캐스트한다.

현재 Vue2 호출 대상은 `NEXT_APP_ORIGIN.LOCAL`이다.
Vue2 테스트 화면은 URL의 기존 `Authorization` 키가 있으면 그 mock 토큰을 POST 헤더에 사용한다.
`test-merchant-token-A`는 `merchantCodeMockData`, `test-merchant-token-B`는
`merchantCodeMockDataB`로 mock session API에서 매핑한다. 쿼리가 없으면 기존 테스트 토큰을 유지한다.
실제 로그인 저장소와 SSE 허브는 이 테스트에서 변경하지 않는다.

| 테스트 창   | URL 경로와 쿼리                                              | 예상 수신  |
| ----------- | ------------------------------------------------------------ | ---------- |
| A 단말 1    | `/?deviceSerialNo=A-device-1`                                | A 알림만   |
| A 단말 2    | `/?deviceSerialNo=A-device-2`                                | A 알림만   |
| B 단말 1    | `/?deviceSerialNo=B-device-1`                                | B 알림만   |
| Vue2 A 발신 | `/npay/connect/sse-test?Authorization=test-merchant-token-A` | A 단말 2대 |
| Vue2 B 발신 | `/npay/connect/sse-test?Authorization=test-merchant-token-B` | B 단말 1대 |

### 비동기 mock API

| API                                              | 입력                             | 성공 응답                                                  | 실패  |
| ------------------------------------------------ | -------------------------------- | ---------------------------------------------------------- | ----- |
| `GET /api/mock/admin/devices?deviceSerialNo=...` | `deviceSerialNoMockData`         | `{ deviceSerialNo, merchantCode: "merchantCodeMockData" }` | `404` |
| `GET /api/mock/session/me`                       | `Bearer test-merchant-token-001` | `{ merchantCode: "merchantCodeMockData" }`                 | `401` |

`lib/connectIdentity.ts`는 객체 매핑을 직접 조회하지 않고 위 API를 `await fetch()`한다. 운영 전환 시 fetch URL과
응답 계약을 실제 어드민/세션 API로 교체한다.

### 1:N SSE 허브

```ts
type MerchantCodeChannel = {
  clients: Map<number, Client>;
  revision: number;
  lastEvent: ConnectEvent | null;
};

Map<merchantCode, MerchantCodeChannel>;
```

- 여러 `deviceSerialNo`가 같은 `merchantCode`로 조회되면 각각 별도 clientId로 같은 채널에 등록된다.
- Vue2 알림 한 번은 해당 채널의 모든 client에 전달된다.
- 다른 merchantCode 채널은 순회하지 않는다.
- revision과 lastEvent도 채널별로 유지해 catch-up 시 다른 사업자의 이벤트가 노출되지 않는다.

## 5. API 계약과 검증

| 호출 주체 | API                                          | 검증                                 | 결과                            |
| --------- | -------------------------------------------- | ------------------------------------ | ------------------------------- |
| 단말      | `GET /api/connect/state?deviceSerialNo=...`  | mock admin API로 단말 소속 확인      | merchant별 revision/lastEvent   |
| 단말      | `GET /api/connect/events?deviceSerialNo=...` | 스트림 시작 전 mock admin API 호출   | merchant 채널에 SSE client 등록 |
| Vue2      | `OPTIONS /api/connect/notify`                | Authorization/Content-Type CORS 허용 | `204`                           |
| Vue2      | `POST /api/connect/notify`                   | mock session API로 token 검증        | merchant 채널에만 브로드캐스트  |

- deviceSerialNo 누락: `400`
- 미등록 단말: connect API에서 `403`
- Vue2 토큰 누락/불일치: `/notify`에서 `401`
- `deviceSerialNo`는 등록 식별자이며 강한 기기 인증 수단은 아니다.

## 6. 변경 파일

### Next.js 저장소

- `app/page.tsx`: 단말 식별, 등록 사전 확인, merchant별 state/events 호출, 상태 UI, watchdog/catch-up
- `app/api/connect/events/route.ts`: 단말 소속 검증, merchant별 SSE 등록, heartbeat
- `app/api/connect/state/route.ts`: 단말 소속 검증, merchant별 최신 상태 응답
- `app/api/connect/notify/route.ts`: Vue2 token 검증, merchant별 브로드캐스트, CORS Authorization 허용
- `app/api/mock/admin/devices/route.ts`: mock 단말 소속 조회
- `app/api/mock/session/me/route.ts`: mock 로그인 세션 조회
- `lib/connectIdentity.ts`: mock API 공통 비동기 fetch 및 merchantCode 추출
- `lib/sseHub.ts`: merchant별 clients/revision/lastEvent 관리
- `app/docs/page.tsx`: 2026-09-22 문서 그룹 추가
- `public/docs/merchant-sse-at-a-glance.html`: 단말 3대의 연결, 사업자별 clients, Vue2 POST, SSE 전송을 한 화면에 보여 주는 요약 도식
- `public/docs/merchant-device-sse-flow.html`: 기존/현재 통신 구조 SVG 비교, 코드 줄별 설명, 실행 순서와 점검 사항을 담은 상세 문서
- `public/docs/realtime-delivery-options.html`: 현재 단일 프로세스 SSE의 병목과 SSE 수평 확장·Polling·MQTT·WebSocket·관리형 서비스의 흐름/장단점/선택 기준 비교
- `public/docs/aws-realtime-architecture-decision.html`: 현재 SSE 문제, ALB·ECS/Fargate SSE Gateway·Redis 방식과 AWS AppSync Events 방식의 구현·보안·복구·비용·선택 기준 상세 비교
- `README.md`: PoC 값, mock API, Vue2 호출 예시

### Vue2 manager 저장소

- `src/pages/naver_pay/NpayConnectSseTest.vue`: `/api/connect/notify` 호출에 PoC Bearer token 추가,
  호출 대상을 `NEXT_APP_ORIGIN.LOCAL`로 설정

## 7. 완료 조건

- [x] heartbeat와 watchdog으로 조용한 연결 단절을 감지한다.
- [x] 재연결 전 기존 EventSource를 정리한다.
- [x] 최초 연결은 baseline만 맞추고 재연결/화면 복귀는 lastEvent를 catch-up한다.
- [x] 단말 소속을 비동기 mock admin API로 확인한다.
- [x] Vue2 로그인 사업자를 비동기 mock session API로 확인한다.
- [x] 동일 merchantCode의 여러 단말을 하나의 채널에 등록한다.
- [x] clients, revision, lastEvent를 merchantCode별로 격리한다.
- [x] Vue2 알림을 검증된 merchantCode 채널에만 전송한다.
- [x] 기존 쿼리 키로 다중 브라우저 테스트 단말/발신 토큰을 선택한다.
- [x] 로컬 HTTP 요청에서 A 알림은 A 단말 2대에만, B 알림은 B 단말 1대에만 도착한다.
- [ ] Chrome 시크릿 창에서 Vue2 버튼과 Next.js 단말 화면의 실제 표시를 확인한다.
- [ ] 실제 WebView에서 `window.npayContext.deviceSerialNo`를 사용해 확인한다.
- [ ] Vue2와 여러 실제 단말을 연결해 1:N 전달을 확인한다.
- [ ] 커밋 전 독립 리뷰를 수행한다.
- [ ] 사용자 커밋 승인을 반영한다.

## 8. 검증 결과

| 검증                           | 대상                                                                                                      | 결과                                                          |
| ------------------------------ | --------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------- |
| ESLint                         | Next.js 관련 TS/TSX 및 docs 페이지                                                                        | passed                                                        |
| TypeScript                     | `npx tsc --noEmit`                                                                                        | passed                                                        |
| Production build               | `npm run build`                                                                                           | passed; connect 3개, mock 2개 동적 Route 및 `/docs` 생성 확인 |
| Diff whitespace                | `git diff --check`                                                                                        | passed                                                        |
| HTML/SVG 구조 점검             | `html.parser`, 앵커·중복 ID·표 닫힘, SVG XML 파싱                                                         | passed; 기존/현재 통신 도식 2개 및 코드 해설 확인             |
| SSE 요약 문서 구조             | HTML/SVG 파싱, 단말 3대·핵심 레이블·중복 ID·`/docs` 링크 확인                                             | passed                                                        |
| 실시간 방식 비교 문서          | 현재 문제, 5개 대안 흐름 SVG, 장단점, 비교표, 권장안 및 `/docs` 링크                                      | passed                                                        |
| AWS 상시 연결 비교 문서        | 현재 SSE, SSE Gateway, AppSync Events SVG와 구현·보안·비용·결론 및 `/docs` 링크                           | passed                                                        |
| SSE 요약 문서 화면 확인        | 브라우저에서 로컬 HTML 열기                                                                               | not-run; 브라우저 URL 보안 정책으로 `file:` 접근 차단         |
| Vue2 format                    | `pnpm prettier --check src/pages/naver_pay/NpayConnectSseTest.vue`                                        | passed                                                        |
| Vue2 type check                | `pnpm typecheck`                                                                                          | passed                                                        |
| 로컬 API 수동 호출             | `npm run dev`                                                                                             | not-run; 실행 환경 포트 바인딩 `listen EPERM`                 |
| 실제 WebView/다중 단말         | 화면 on/off, 백그라운드, 1:N 전송                                                                         | not-run                                                       |
| 다중 브라우저 테스트 정적 검사 | Next.js 변경 파일 ESLint·`npx tsc --noEmit`, Vue2 Prettier·`pnpm typecheck`, 두 저장소 `git diff --check` | passed; `78950a4` + 현재 작업 트리                            |
| 사업자별 SSE HTTP 연동         | 로컬 `localhost:3000`에 단말 3개 GET 연결 후 A/B 토큰으로 각각 POST                                       | passed; A 단말 2개만 A 수신, B 단말 1개만 B 수신              |
| Chrome 시크릿 창 UI            | Vue2 버튼 클릭과 단말별 화면 갱신                                                                         | not-run; 수동 브라우저 확인 필요                              |

## 9. 현재 상태와 잔여 위험

- 상태: `awaiting_review` (다중 브라우저 PoC 구현과 실행 가능한 검증 완료)
- 독립 리뷰: not-run
- local commit: Next.js `78950a4` (이번 추가 작업의 commit은 없음)
- push: 미수행
- 차단 사항: 없음

잔여 위험:

- `globalThis` 허브는 단일 Node.js 프로세스에서만 동작한다. 서버리스/멀티 인스턴스에서는 Redis 등 공용
  Pub/Sub과 상태 저장소가 필요하다.
- mock API와 고정 token은 운영 인증이 아니다.
- `deviceSerialNo`만으로는 요청자가 실제 단말임을 강하게 증명하지 못한다.
- CORS가 `*`이며 운영 origin/credential/CSRF 정책이 적용되지 않았다.
- Vue2 성공 응답은 서버 enqueue 완료를 뜻하며 각 단말의 수신·렌더 완료를 보장하지 않는다.
- 실제 단말의 heartbeat/watchdog 임계값 적합성은 확인하지 않았다.
- heartbeat는 클라이언트 watchdog의 활동 신호이며 서버 cancel을 즉시 보장하지 않는다. 네트워크 단절 감지는
  런타임에 따라 지연될 수 있다.
- catch-up은 merchant별 최신 화면 이벤트 1건만 복구하며 이벤트 이력 전체를 순차 재생하지 않는다.

## 10. 사용자 후속 작업

1. 작업번호 `FE-879` 확정 또는 정정
2. 커밋 전 독립 리뷰 요청
3. 실제 WebView에서 고정 mock 시리얼을 `window.npayContext.deviceSerialNo`로 교체해 테스트
4. 동일 merchantCode에 여러 단말을 매핑하여 1:N 전달 및 다른 merchant 격리 확인
5. 변경사항 확인 후 commit 명시 승인
