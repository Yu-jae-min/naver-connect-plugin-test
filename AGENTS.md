# Codex 프로젝트 공통 작업 지침

이 파일은 저장소 루트에 두고 해당 프로젝트의 모든 Codex 작업에 공통으로 적용한다.
프로젝트별 기술·아키텍처·검증 규칙은 이 파일에 작성하고, 작업별 요구사항과 현재 상태는
루트의 `FE-*.md` 형식 문서에서 관리한다.

## 지침과 작업 브리프 확인

1. 사용자 최신 지시를 가장 우선한다.
2. 작업을 시작하기 전에 저장소 루트부터 현재 작업 경로까지 적용되는 `AGENTS.md`와 `AGENTS.override.md`를 확인한다.
3. 사용자가 작업 브리프를 지정했다면 해당 파일을 읽는다.
4. 지정하지 않았다면 현재 브랜치명에서 작업번호를 추출해 같은 번호의 브리프를 선택한다.
   - 예: `feature/FE-123` → `FE-123.md`
   - 일치하는 문서가 없거나 여러 문서가 충돌하면 임의로 선택하지 않고 사용자에게 확인한다.
5. `<...>` placeholder는 요구사항이 아니다.
6. `미정`은 결정이 필요한 값이고, `Codex가 조사`는 저장소나 제공 자료에서 확인할 값이다.
7. 사용자 최신 지시는 현재 작업의 최우선 기준이다. `AGENTS.md`는 프로젝트 구현 제약의 기준으로,
   작업 브리프는 feature 범위와 완료 조건의 기준으로 사용한다. 서로 충돌하거나 제품 결정이
   필요하면 임의로 우선순위를 해석하지 않고 충돌 지점과 영향을 보고한다.

## 세션 시작과 재개 절차

작업을 시작하거나 새 세션에서 재개할 때 다음을 순서대로 확인한다.

1. 현재 브랜치
2. `git status --short`로 staged·unstaged·untracked 변경 확인
3. 최근 관련 commit
4. 작업 브리프의 기준 commit과 작업 시작 당시 기존 변경
5. 작업 브리프의 현재 상태, 남은 작업, 차단 사항
6. 관련 구현과 테스트 설정

작업 브리프의 상태 기록은 인계 정보다. 실제 Git 상태나 코드와 충돌하면 Git과 코드를 근거로 사실을 다시 확인하고, 구현 전에 브리프의 오래된 상태를 최신 정보로 교체한다.

이미 완료된 구현이나 검증을 이유 없이 반복하지 않는다. 다만 마지막 검증 이후 코드가 변경됐거나 검증 증거가 불명확하면 영향을 받는 검증을 다시 실행한다.

## 조사와 변경 원칙

1. 구현 전에 현재 branch, status, diff와 기존 사용자 변경사항을 확인하고 보존한다.
2. README, package manifest, lockfile, 빌드·테스트 설정과 기존 코드를 통해 프로젝트 구조와 실제 명령을 확인한다.
3. 프레임워크, 버전, 패키지 매니저, 실행 명령과 기존 동작을 추측하지 않는다.
4. 빈 선택 정보 때문에 작업을 중단하지 않는다. 읽기 전용 조사와 안전한 기본값으로 진행할 수 있으면 계속한다.
5. 제품 UX, API 계약, 호환성, 보안, 저장 데이터, 외부 연동 또는 배포 결정이 필요하면 사용자에게 확인한다.
6. 현재 코드 패턴을 따르는 가장 작은 변경으로 요구사항을 충족한다.
7. 요청하지 않은 마이그레이션, 의존성 교체, 대규모 리팩터링, 불필요한 추상화와 대규모 포맷 변경을 하지 않는다.
8. 작업 범위 밖의 기존 변경을 제거하거나 덮어쓰거나 함께 정리하지 않는다.

## 포맷과 변경 범위

- 프로젝트에 존재하는 포맷 설정과 기존 파일 스타일을 기준으로 한다.
- 저장 시 포맷터가 파일 전체를 변경할 수 있으므로 작은 값 수정에서는 포맷 노이즈를 확인한다.
- 기존 파일 전체가 포맷 규칙과 다르더라도 작업과 관계없는 줄을 일괄 포맷하지 않는다.
- 필요한 경우 에디터의 `Save without Formatting`을 사용하거나 의도한 hunk만 반영한다.
- 포맷 전후에 `git diff`로 의미 없는 줄바꿈 변경이 섞였는지 확인한다.
- 자동 생성 파일과 바이너리 fixture는 작업에 필요한 경우에만 변경하고, 필요한 이유를 보고한다.

## 구현과 검증

1. 작업 브리프의 포함 범위와 완료 조건을 기준으로 구현한다.
2. 코드 변경 후 영향 범위와 회귀 위험을 확인한다.
3. 프로젝트에서 확인한 명령 중 가장 좁고 관련성이 높은 테스트와 정적 검사부터 실행한다.
4. 실행하지 않은 검사는 통과로 표시하지 않는다. 실행하지 못한 이유와 잔여 위험을 `not-run`으로 보고한다.
5. 구현자의 판단과 실제 검증 결과를 구분한다.
6. 명백히 깨졌거나 불완전한 상태를 완료로 처리하지 않는다.
7. 검증이 끝나면 변경사항, 검증 결과, 미검증 영역과 잔여 위험을 사용자에게 간결하게 보고한다.

## 커밋 전 독립 리뷰

1. 코드 변경의 구현과 실행 가능한 검증이 끝나면 상태를 `awaiting_review`로 변경하고
   사용자에게 커밋 전 독립 리뷰가 필요함을 보고한다.
2. 사용자가 커밋 전 독립 리뷰를 요청하면 현재 작업 브리프와 저장소에 있는
   `code-review-template.md`를 읽고 해당 리뷰 절차를 따른다.
3. 리뷰 템플릿의 경로를 사용자가 지정하지 않았다면 저장소에서 이름이 일치하는 파일을 찾는다.
   파일이 없거나 여러 개라서 적용할 템플릿을 확정할 수 없으면 임의로 선택하지 않고 사용자에게 확인한다.
4. 리뷰 중에는 코드, 테스트, 문서와 설정 파일을 수정하거나 생성하지 않는다.
5. 리뷰 결과를 보고한 뒤 구현 세션 또는 사용자가 작업 브리프에 결과를 반영한다.

## 작업 브리프 관리

1. 최초 구현, 추가 요청, 수정, 검증과 완료까지 같은 작업 브리프를 갱신한다.
2. 추가 요청이 같은 작업의 연장이면 새 문서를 만들지 않고 범위, 완료 조건, 검증 계획과 상태를 현재 기준으로 갱신한다.
3. 대화 전체나 폐기된 구현 과정을 작업 로그처럼 누적하지 않는다. 다시 조사할 필요가 없는 확인된
   사실과 현재 구현·리뷰에 영향을 주는 범위 변경 및 결정은 브리프에 간결하게 남긴다.
4. 다음에 해당하면 별도 작업 분리를 제안한다.
   - 독립적으로 배포하거나 롤백할 수 있는 신규 기능
   - 다른 도메인·화면·앱·서비스로 크게 확장되는 변경
   - 별도 기획·디자인·API 결정이 필요한 변경
   - 현재 작업의 위험도나 검증 범위를 크게 늘리는 변경
5. 사용자가 현재 작업에 포함하기로 결정하면 브리프의 범위와 완료 조건을 갱신한 뒤 진행한다.

## 작업 상태

작업 브리프의 상태는 다음 값을 사용한다.

- `planned`: 작업 요청과 범위가 작성된 상태
- `investigating`: 프로젝트와 기존 동작을 조사 중인 상태
- `implementing`: 코드를 변경 중인 상태
- `verifying`: 자동 또는 수동 검증 중인 상태
- `awaiting_review`: 구현과 실행 가능한 검증이 끝나 독립 리뷰를 기다리는 상태
- `reviewed`: 독립 리뷰와 필요한 수정·재검증이 끝나 사용자 확인을 기다리는 상태
- `approved_for_commit`: 사용자가 현재 변경의 commit을 명시적으로 승인한 상태
- `blocked`: 사용자 결정이나 외부 조건 없이는 진행할 수 없는 상태
- `completed`: 요청한 결과와 실행 가능한 검증 및 결과 기록이 끝나고, 코드 변경이 있는
  경우 사용자의 commit 결정까지 반영된 상태

상태, 완료한 핵심 작업, 남은 작업, 차단 사항과 이후 작업에 필요한 결정만 간결하게 기록한다.

## Git과 Commit

### 기본 원칙

- 최초 작업 요청과 추가 요청은 구현과 검증에 대한 승인이지 stage 또는 commit 승인이 아니다.
- 사용자가 현재 변경사항을 리뷰하고 commit을 명시적으로 승인하기 전에는 stage하거나 commit하지 않는다.
- 승인 후에는 이번 작업에 해당하는 파일 또는 hunk만 stage한다. `git add .`처럼 범위 밖 변경이 섞일 수 있는 방식은 피한다.
- local commit까지만 수행한다. push와 이후 Git 흐름은 사용자가 담당한다.
- 사용자가 별도로 명시하지 않는 한 merge, rebase, cherry-pick, revert, reset, amend, force push, branch 생성·삭제·이동과 remote 변경을 하지 않는다.

### 기본 커밋 제외 대상

사용자가 명시적으로 포함하라고 요청하지 않는 한 다음 로컬 작업 문서는 stage하거나 commit하지 않는다.

- `AGENTS.md`
- `FE-*.md`
- `code-review-template.md`

### Commit 전 확인

다음 조건을 모두 만족한 경우에만 local commit을 생성한다.

1. 독립적으로 설명 가능한 변경이 완결되었다.
2. 적용 가능한 가장 좁은 검증을 수행했다.
3. 변경사항, 검증 결과, 미검증 영역과 잔여 위험을 사용자에게 보고했다.
4. 독립 리뷰의 전체 판정이 `Pass`다.
5. 사용자가 현재 변경사항을 리뷰하고 commit을 명시적으로 승인했다.
6. 작업 브리프의 작업 브랜치와 현재 브랜치가 일치한다.
7. 독립 리뷰 이후 commit 대상 파일 또는 hunk에 변경이 있었다면 필요한 재리뷰 또는 재검증을 완료했다.
8. 승인 이후 리뷰되지 않은 새 변경이 추가되지 않았다.
9. stage할 변경이 작업 범위에 속한다.

승인 이후 변경이 추가되었으면 commit을 보류하고 차이를 보고한 뒤 재리뷰와 재승인을 기다린다.
작업 브리프의 상태·리뷰·결과 기록만 갱신한 변경은 재리뷰 대상 변경으로 간주하지 않는다.
검증을 실행할 수 없어 독립 리뷰가 `Pass`가 아니면 기본적으로 commit을 보류하고 그 이유와 위험을
보고한다. 사용자가 미검증 위험을 확인한 뒤 기본 조건의 예외로 commit을 명시적으로 승인한 경우에만 진행한다.

### Commit 단위

- 파일이나 체크박스 개수가 아니라 하나의 논리적인 기능 또는 변경 목적을 기준으로 나눈다.
- 여러 파일이 한 기능을 함께 구현하면 하나의 commit으로 묶을 수 있다.
- 서로 독립적인 기능은 같은 파일 안의 변경이어도 분리할 수 있다.
- 구현과 직접 연결된 테스트는 해당 구현 commit에 포함하는 것을 우선한다.
- 독립적인 테스트 보강은 별도 `Test` commit으로 분리할 수 있다.
- 추가 요청은 기존 commit을 amend하지 않고, 재리뷰와 승인 후 새 local commit으로 남긴다.

### Commit 메시지

프로젝트에 별도 규칙이 있으면 그 규칙을 우선한다. 별도 규칙이 없으면 다음 형식을 사용한다.

```text
<작업번호>(<Type>): <한글 요약>
```

Type은 `Feat`, `Fix`, `Style`, `Refactor`, `Test`, `Docs`, `Chore` 중 변경 목적에 맞는 값을 사용한다. 제목은 변경 목적이 드러나는 간결한 한글로 쓰고 마침표를 붙이지 않는다. 작업번호를 확인할 수 없으면 임의로 만들지 않고 commit을 보류한다.

## 완료 보고

작업 완료 시 브리프의 작업 결과에 다음만 기록한다.

- 사용자 관점의 최종 결과
- 핵심 변경 파일과 목적
- 생성한 local commit 또는 `없음`
- branch, push 미수행 여부, 남은 uncommitted 변경과 보존한 범위 밖 변경
- 주요 구현 판단·결정
- 독립 리뷰의 대상 revision, 판정과 주요 findings
- 검증별 명령·시나리오, 대상 revision, 증거와 `passed / failed / not-run` 결과
- 미검증 영역과 잔여 위험
- 사용자 후속 작업
